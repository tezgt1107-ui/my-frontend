(() => {const $=s=>document.querySelector(s);$('#guidGenerate').onclick=()=>{const n=Math.min(100,Math.max(1,Number($('#guidCount').value)||1)),up=$('#guidUpper').checked;$('#guidOutput').value=Array.from({length:n},()=>{const v=crypto.randomUUID();return up?v.toUpperCase():v}).join('\n');};$('#passwordGenerate').onclick=()=>{const len=Math.min(128,Math.max(8,Number($('#passwordLength').value)||20)),avoid=$('#passwordAvoid').checked;let chars=avoid?'ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz23456789':'ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789';if($('#passwordSymbols').checked)chars+='!@#$%^&*_-+=?';const arr=new Uint32Array(len);crypto.getRandomValues(arr);$('#passwordOutput').value=[...arr].map(x=>chars[x%chars.length]).join('');};$('#guidGenerate').click();$('#passwordGenerate').click();})();
