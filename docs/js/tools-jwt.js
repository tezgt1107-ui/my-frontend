(() => {const $=s=>document.querySelector(s);const decode=s=>decodeURIComponent(atob(s.replace(/-/g,'+').replace(/_/g,'/').padEnd(Math.ceil(s.length/4)*4,'=')).split('').map(c=>'%'+c.charCodeAt(0).toString(16).padStart(2,'0')).join(''));$('#jwtDecode').onclick=()=>{try{const p=$('#jwtInput').value.trim().split('.');if(p.length<2)throw new Error('JWT 格式必須至少有兩個區段');const h=JSON.parse(decode(p[0])),body=JSON.parse(decode(p[1]));$('#jwtHeader').value=JSON.stringify(h,null,2);$('#jwtPayload').value=JSON.stringify(body,null,2);let s='解析成功（尚未驗證簽章）';if(body.exp){const exp=new Date(body.exp*1000);s+=`｜到期：${exp.toLocaleString('zh-TW',{hour12:false})}｜${exp<Date.now()?'已過期':'仍有效'}`;}$('#jwtStatus').textContent=s;}catch(e){$('#jwtStatus').textContent='錯誤：'+e.message;}};$('#jwtClear').onclick=()=>{$('#jwtInput').value=$('#jwtHeader').value=$('#jwtPayload').value='';$('#jwtStatus').textContent='等待輸入';};})();
