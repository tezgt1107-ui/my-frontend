(() => {const $=s=>document.querySelector(s);let last=[];const words=s=>s.trim().split(/[^a-zA-Z0-9\u4e00-\u9fff]+/).filter(Boolean);const cv=(s,t)=>{const w=words(s);if(t==='upper')return s.toUpperCase();if(t==='lower')return s.toLowerCase();if(t==='camel')return w.map((x,i)=>i?x[0].toUpperCase()+x.slice(1).toLowerCase():x.toLowerCase()).join('');if(t==='pascal')return w.map(x=>x[0].toUpperCase()+x.slice(1).toLowerCase()).join('');if(t==='snake')return w.join('_').toLowerCase();if(t==='kebab')return w.join('-').toLowerCase();return s;};function process(){let a=$('#textInput').value.split(/\r?\n/);if($('#textTrim').checked)a=a.map(x=>x.trim());if($('#textRemoveEmpty').checked)a=a.filter(Boolean);if($('#textDistinct').checked)a=[...new Set(a)];a=a.map(x=>$('#textPrefix').value+cv(x,$('#textCase').value)+$('#textSuffix').value);last=a;$('#textOutput').value=a.join('\n');$('#textStatus').textContent=`${a.length} 行`;};$('#textProcess').onclick=process;$('#textSort').onclick=()=>{process();last.sort((a,b)=>a.localeCompare(b,undefined,{numeric:true}));$('#textOutput').value=last.join('\n');};$('#textReverse').onclick=()=>{process();$('#textOutput').value=last.reverse().join('\n');};})();
