(() => {const $=s=>document.querySelector(s),i=$('#encodeInput'),o=$('#encodeOutput'),st=$('#encodeStatus');const ok=(v,m)=>{o.value=v;st.textContent=m};$('#base64Encode').onclick=()=>{try{ok(btoa(unescape(encodeURIComponent(i.value))),'Base64 編碼完成')}catch(e){st.textContent=e.message}};$('#base64Decode').onclick=()=>{try{ok(decodeURIComponent(escape(atob(i.value.trim()))),'Base64 解碼完成')}catch(e){st.textContent='無效的 Base64'}};$('#urlEncode').onclick=()=>ok(encodeURIComponent(i.value),'URL 編碼完成');$('#urlDecode').onclick=()=>{try{ok(decodeURIComponent(i.value),'URL 解碼完成')}catch(e){st.textContent='無效的 URL 編碼'}};$('#sha256').onclick=async()=>{const b=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(i.value));ok([...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,'0')).join(''),'SHA-256 完成');};})();
