(() => {const $=s=>document.querySelector(s);$('#diffCompare').onclick=()=>{const a=$('#diffLeft').value.split(/\r?\n/),b=$('#diffRight').value.split(/\r?\n/),n=Math.max(a.length,b.length),norm=s=>{if($('#diffIgnoreWhitespace').checked)s=s.trim();if($('#diffIgnoreCase').checked)s=s.toLowerCase();return s};let html='';for(let i=0;i<n;i++){const l=a[i]??'',r=b[i]??'',same=norm(l)===norm(r),lc=i>=a.length?'diff-add':i>=b.length?'diff-remove':same?'':'diff-change',rc=i>=b.length?'diff-remove':i>=a.length?'diff-add':same?'':'diff-change';html+=`<div class="diff-row"><div class="diff-num">${i<a.length?i+1:''}</div><div class="${lc}">${esc(l)}</div><div class="diff-num">${i<b.length?i+1:''}</div><div class="${rc}">${esc(r)}</div></div>`;}$('#diffOutput').innerHTML=html||'<div class="empty-state">沒有內容</div>';};const esc=s=>s.replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));})();
